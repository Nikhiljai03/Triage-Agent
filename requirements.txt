# ---------------------------------------------------------------------------
# Superset for LOCAL DEV / FULL TEST RUNS — `pip install -r requirements.txt`
# installs everything (api + worker + dev tooling), so the full test suite and
# CI work with a single install.
#
# The Docker images install the per-service SUBSETS instead (faster, smaller):
#   api/Dockerfile    -> requirements/api.txt    (light: no torch/langgraph)
#   worker/Dockerfile -> requirements/worker.txt (full agent stack incl. torch)
#
# Pins live in requirements/{base,api,worker}.txt — edit there, not here.
# ---------------------------------------------------------------------------
-r requirements/api.txt
-r requirements/worker.txt

# Dev / test only (not needed in either image):
pytest==9.1.1
fakeredis==2.36.2          # in-memory Redis for offline tests
