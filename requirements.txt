# ---------------------------------------------------------------------------
# Phase 0 — service skeleton.
# ---------------------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7

# ---------------------------------------------------------------------------
# Phase 1 — RAG ingestion + retrieval (installed, version-pinned).
#
# NOTE: sentence-transformers pulls in torch and is HEAVY (hundreds of MB). The
# api/worker images still build with it, but only the ingestion CLI
# (`python -m rag.ingest`) actually needs it at runtime. The FIRST run downloads
# the embedding model (all-MiniLM-L6-v2, ~90 MB) and the cross-encoder reranker
# (ms-marco-MiniLM-L-6-v2) into the HuggingFace cache (~/.cache/huggingface).
# ---------------------------------------------------------------------------
qdrant-client==1.18.0
sentence-transformers==5.6.0
tiktoken==0.13.0
PyGithub==2.9.1
openai==2.44.0

# ---------------------------------------------------------------------------
# Dev / test dependencies.
# ---------------------------------------------------------------------------
pytest==9.1.1

# ---------------------------------------------------------------------------
# Roadmap below: intentionally NOT installed yet. Uncomment per phase.
# ---------------------------------------------------------------------------

# Phase 2 — GitHub ingestion via webhooks
# httpx>=0.28

# Phase 3 — Sandbox (Dockerized bug reproduction)
# docker>=7.1                        # Docker SDK for Python

# Phase 4 — Agent orchestration
# langgraph>=0.2
# langchain-core>=0.3

# Phase 5 — Queue / background processing
# redis>=5.2
# rq>=2.0

# Phase 6 — Evaluation
# pandas>=2.2                        # metric aggregation / reporting
